import { styled } from 'styled-components'

export const SkItemStyled = styled.div`
  width: 100%;
  height: 100%;
  position: relative;
  cursor: pointer;
  border-radius: var(--radius);
  overflow: hidden;
  background-color: var(--color-text-light);
  opacity: 30%;
`
